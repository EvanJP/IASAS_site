<%= form_for current_user, :html => {:class => 'col s12 m8 l8'}  do |f| %>
<div class="row">
    <div class="input-field col s12">
        <%= fields_for(@user_events) do |ue| %>
            <%= collection_select(:events, :id, @all_events, :id, :event, {}, {:multiple => true}) %>
        <% end %>
        <%= f.submit "Save changes", class: "btn waves-effect waves-light"%>
        <% end %>
    </div>
</div>
